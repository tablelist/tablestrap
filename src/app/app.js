angular.module('tablestrapApp', [
  'ui.router'
]);

angular.module('tablestrapApp').run([
  '$rootScope',
  '$state',
  '$stateParams',
  function($rootScope, $state, $stateParams) {
    'use strict';
  }
]);
